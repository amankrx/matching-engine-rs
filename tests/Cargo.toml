[package]
name = "tests"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
itch-parser = { path = "../itch-parser" }
optimized-lob = { path = "../optimized-lob" }